<!DOCTYPE html>
<html lang="zh-cn">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "TickTock" %></title>
  <%= csrf_meta_tags %>

  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= yield :style %>

  <link href="images/favicon.ico" rel="shortcut icon">
  <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
  <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
  <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
</head>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>

        <a class="brand" href="/">点滴</a>

        <div class="nav-collapse">
          <ul class="nav">
            <% if user_signed_in? %>
              <li><a href="<%= moments_path %>">我的点滴墙</a></li>
              <li><a href="<%= new_moment_path %>">新的点滴</a></li>
            <% end %>
          </ul>
          <ul class="nav pull-right">
            <% unless user_signed_in? %>
              <li <%= 'class=active' if current_page?(:controller => 'registrations', :action => 'new') %>>
                <%= link_to t('devise.navigation.sign_up'), new_user_registration_path %>
              </li>
              <li <%= 'class=active' if current_page?(:controller => 'sessions', :action => 'new') %>>
                <%= link_to t('devise.navigation.sign_in'), new_user_session_path %>
              </li>
            <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <%= (current_user.name.blank?) ? current_user.email : current_user.name %>
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to(t('devise.navigation.change_registration'),edit_user_registration_path) %></li>
                  <li><%= link_to t('devise.navigation.sign_out'), destroy_user_session_path, :method => :delete %></li>
                </ul>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <div class="container">
    <%= render 'shared/notice', :span => 'span10 offset1' %>

    <%= yield %>

    <footer class="footer">
      <hr />
      <div class="pull-left" style="margin: 0 10px 0 10px;">
        <%= image_tag 'rails.png', :width => 30 %>
      </div>
      <div>
        <p>
          Created by &copy; <a href="http://jasl.github.com">Jasl</a> 2012.<br/>
          Based on rails 3.2. Watch the source code on <a href="https://github.com/jasl/tick-tock">github</a>.
        </p>
      </div>
    </footer>
  </div>

  <%= javascript_include_tag "application" %>
  <%= yield :js %>
</body>
</html>
